# Trained Reader artifacts go here (reader_issue.keras, reader_frustration.keras,
# tokenizer.json, label_maps.json). Currently MISSING - must be produced by
# train_reader.py before the Reader can run. See docs/module1_steering_context.md.
